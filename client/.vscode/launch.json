{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "pwa-chrome",
        "request": "launch",
        "name": "Launch Client",
        "url": "http://localhost:3000",
        "webRoot": "${workspaceFolder}/src"
      }, {
        "type": "chrome",
    "request": "launch",
    "name": "Debug React Run",
    "url": "http://localhost:3000",
    "webRoot": "${workspaceFolder}/src"
      }
    ]
  }