overwrite: true
schema: 
   - "./src/schema/definition/*.ts"
documents: null
generates:
  src/schema/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  graphql.schema.json:
    plugins:
      - "introspection"